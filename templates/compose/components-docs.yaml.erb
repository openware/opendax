version: '3.6'

services:
  components-docs:
    image: "<%= @config['images']['components-docs'] %>"
    expose:
      - "80"
    labels:
      traefik.enable: true
      traefik.frontend.rule: 'PathPrefix: /components, /src;Host: <%= @config['app']['subdomain'] %>.<%= @config['app']['domain'] %>'
      traefik.port: 80
      traefik.frontend.auth.basic: 'ILoveOpenware:$$apr1$$F47ty7.m$$PmAZpvByLBNgxNld4m1B81'