version: '3.6'

services:
  components-docs:
    image: "<%= @config['images']['components-docs'] %>"
    expose:
      - "80"
    labels:
      - "traefik.http.routers.frontend.rule=Host(`<%= @config['app']['subdomain'] %>.<%= @config['app']['domain'] %>`) && PathPrefix(`/components, /src`)"
      - "traefik.http.routers.frontend.entrypoints=web"
      - "traefik.enable=true"
      - "traefik.http.services.frontend.loadbalancer.server.port=80"
      - "traefik.http.routers.frontend.middlewares=auth"
      - "traefik.http.middlewares.auth.basicauth.users=ILoveOpenware:$$apr1$$F47ty7.m$$PmAZpvByLBNgxNld4m1B81"
